#include <stdio.h>
int no;
int main(){

printf("Enter a Number \n");
scanf("%d",&no);
printf("Divisibility test returns %d\n", 97%97);
printf("If the value is non zero, it is not divisible and if value is 0 then its divisible");
return 0;
}
